<ui version="4.0" >
 <class>generalui</class>
 <widget class="QWidget" name="generalui" >
  <property name="geometry" >
   <rect>
    <x>0</x>
    <y>0</y>
    <width>306</width>
    <height>172</height>
   </rect>
  </property>
  <property name="toolTip" >
   <string>Here you can choose settings for all languages.</string>
  </property>
  <layout class="QGridLayout" >
   <item row="0" column="0" >
    <layout class="QVBoxLayout" >
     <item>
      <widget class="QCheckBox" name="kcfg_OneLetter" >
       <property name="toolTip" >
        <string>Check this if you do not want to discover each instance of the same letter.</string>
       </property>
       <property name="whatsThis" >
        <string>If you check this box, only the first letter will be written if the letter is found in several places in the word. Then you have to rechoose the same letter, to replace the second instance in the word until there are no more instances of this letter.
For example, the word to guess is "potato". If this option is unchecked, when you try "o", the two instances of "o" in the word will be discovered at the same time. If this is checked, you will need to try "o" twice.

Default is that for one letter typed, all instances of that letter in the word are discovered.</string>
       </property>
       <property name="text" >
        <string>Require more guesses for duplicate letters</string>
       </property>
      </widget>
     </item>
     <item>
      <widget class="QCheckBox" name="kcfg_EnableAnimations" >
       <property name="text" >
        <string>Enable animations</string>
       </property>
      </widget>
     </item>
     <item>
      <widget class="QGroupBox" name="groupBox" >
       <property name="title" >
        <string>Sounds</string>
       </property>
       <layout class="QVBoxLayout" >
        <item>
         <widget class="QCheckBox" name="kcfg_Sound" >
          <property name="toolTip" >
           <string>If checked, sounds will be played for New Game and Win Game</string>
          </property>
          <property name="whatsThis" >
           <string>If this box is checked, you will have some sounds played for each new game and when you win a game. If this is not checked, there will be no sound in KHangMan.
Default is no sound.</string>
          </property>
          <property name="text" >
           <string>Enable sounds</string>
          </property>
         </widget>
        </item>
       </layout>
      </widget>
     </item>
    </layout>
   </item>
   <item row="1" column="0" >
    <spacer>
     <property name="orientation" >
      <enum>Qt::Vertical</enum>
     </property>
     <property name="sizeHint" >
      <size>
       <width>20</width>
       <height>40</height>
      </size>
     </property>
    </spacer>
   </item>
  </layout>
 </widget>
 <resources/>
 <connections/>
</ui>
